<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thomasstep.pomodoronoise">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pomodoro_noise_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pomodoro_noise_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.PomodoroNoise">
        <activity android:name=".TimerActivity"></activity>
        <activity android:name=".PomodoroNoise">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-1006697357328189~2092814638"/>
    </application>

</manifest>